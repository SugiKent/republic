// 「お気に入りに追加」ボタンを押した時の挙動
// 次に表示するのは削除ボタンなので、favorite_button_for_destroyをレンダリング
// 「お気に入りから削除」ボタンのパーシャルを読み込む
// 渡す値は、favo: Favoriteのオブジェクト（favorite_controllerから渡される）
// lesson: Lessonのオブジェクト
// size: ボタンの大きさ（文字）
$("#Favorite__button--<%= @lesson.id %>").html('<%= j(render "favorite_button_for_destroy", favo: @favorite, lesson: @lesson, size: "sm") %>')
